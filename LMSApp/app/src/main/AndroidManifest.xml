<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="scarlet.machado.lmsapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LMSApp">
       <activity android:name=".TelaInicialActivity"/>
        <activity android:name=".ConfigurarActivity"/>
        <activity android:name=".AnotarPedidoActivity" />
        <activity android:name=".AbrirComandaActivity" />
        <activity android:name=".AbrirCardapioActivity" />
        <activity android:name=".AcoesBtnActivity"/>
        <activity android:name=".CadastrarActivity" />
        <activity android:name=".DebugActivity" />
        <activity android:name=".ProdutosActivity"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
